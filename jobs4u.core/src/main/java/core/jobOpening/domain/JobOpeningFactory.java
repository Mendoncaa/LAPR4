/*package core.domain;

import core.util.JobTitle;
import core.util.JobDescription;
import core.util.JobNumberOfVacancies;
import core.util.ContractType;
import core.util.JobMode;
import core.util.JobAddress;


public class JobOpeningFactory {

    public JobOpening createJobOpening(
            JobTitle title,
            JobDescription description,
            JobNumberOfVacancies vacancies,
            ContractType contractType,
            JobMode mode,
            JobAddress address) {
        // Aqui você pode adicionar lógica adicional, como validações, antes de criar a instância de JobOpening
        JobReference reference = new JobReference(); // Gera uma nova referência única
        return new JobOpening(reference, title, description, vacancies, contractType, mode, address);
    }
}
*/
